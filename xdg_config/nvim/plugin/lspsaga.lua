require("lspsaga").setup {
  symbol_in_winbar = {
    enable = false,
  },
  lightbulb = {
    virtual_text = false,
  },
  outline = {
    auto_preview = false, -- don't display symbol preview in the outline window
  },
}
